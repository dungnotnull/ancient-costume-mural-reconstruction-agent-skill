# Core crawl pipeline dependencies
requests>=2.31.0
feedparser>=6.0.10
python-dateutil>=2.8.2

# Optional: rich page scraping for the crawl pipeline
crawl4ai>=0.3.0

# Development / testing tooling (install via `pip install -e .[dev]`)
pytest>=7.4
pytest-cov>=4.1